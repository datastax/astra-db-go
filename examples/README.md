# Examples

Want to run an example? Download or copy/paste the code into a local folder. Then:

```bash
# Initialize a go module
go mod init github.com/example/module
# Grab dependencies
go mod tidy
# Now run the program
go run .
```