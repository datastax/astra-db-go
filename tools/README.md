# tools

Internal code-generation and development tools for the `astra-db-go` module. These are **not** part of the public API and are not included in the compiled library. Similar to `hack/` folder in the [K8s rep](https://github.com/kubernetes/kubernetes/tree/master/hack).